You are working in this git worktree only: C:\WORK\mt-asphalt-wt-backend (branch agent/backend-persistence). Do not touch anything outside this directory.

Context: MT Asphalt is a real small business (owner: Michael Tebb) getting a Vite+React dashboard live for real use within 2 days. Right now ALL business data (customers, jobs, invoices, contracts) lives ONLY in the browser's localStorage (`app/src/lib/store.tsx`). This is documented as a launch blocker in `ASSISTANT_V1.md` ("localStorage is the only copy"): clearing site data, a browser reset, or a new phone loses the entire business's records with no warning and no recovery.

This project already has a working pattern to copy: `app/api/leads.js` is a Vercel serverless function that persists lead data to Vercel Blob storage (`@vercel/blob` is already a dependency; `BLOB_READ_WRITE_TOKEN` is already set in `app/.env.local` for local dev). `store.tsx` already has a `VITE_API_URL` escape hatch mentioned in ASSISTANT_V1.md — check whether it's real or aspirational.

YOUR TASK:
1. Read `app/api/leads.js` closely (the Blob read/write pattern to copy), `app/src/lib/store.tsx` in full (the current single source of truth: what state it holds, how it loads/saves, every place components read/write through it), and `app/src/lib/types.ts`.
2. Add a new serverless function (e.g. `app/api/data.js`) that reads/writes the dashboard's full business-data blob (customers, jobs, invoices, contracts, settings — whatever store.tsx currently persists to localStorage) to Vercel Blob, keyed so each browser/business instance gets its own record (a single fixed key is fine for now since this is a single-owner tool, not multi-tenant).
3. Wire `store.tsx` to write-through: every local mutation still writes to localStorage immediately (instant, offline-first UI, unchanged behavior), AND also fires an async save to `/api/data` (debounced/batched is fine — don't fire one network call per keystroke). On load, if a network fetch to `/api/data` succeeds and has data, prefer it (merge or replace — pick whichever is safe and simple); if the fetch fails (offline, API down, no token), fall through to localStorage exactly as today. This must NEVER be a regression — if the backend is unreachable for any reason, the app must work exactly as it does right now, silently.
4. Do not change the public interface/shape of anything `store.tsx` exports if you can avoid it, so no UI components need to change. Do not touch the Assistant, auth, or seed-data code.
5. When done: run `npm run build` (inside the `app` folder) and fix any TypeScript/build errors until clean. Run `npm run verify` and confirm it still passes. If you can locally sanity-check the new `/api/data` function's logic without a live Vercel dev server, do so; otherwise reason carefully about correctness since this handles real business data.
6. Do NOT commit, do NOT push. Leave the finished, building, working changes in this worktree for review.

When finished, print a short summary of exactly what you changed, note any risk/caveat a human reviewer should know about, and confirm build+verify both pass.
